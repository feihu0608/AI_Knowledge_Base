# 知识库管理平台：实现分析与开发路线

> 更新说明（2026-09-14）：用户后续已确认多租户、单直属部门、部门权限覆盖下级、MinerU API、硅基流动三模型、云部署、7 日业务日志，以及“本地知识 + 魔搭 MCP + LLM”三路方案。最新详细基线请以《01_知识库管理平台_需求规格说明书.md》v1.1 和《02_知识库管理平台_标准AI项目架构设计说明书.md》v3.1 为准；本文保留为早期分析背景，其中“不需要多 Agent”的早期候选结论已被用户后续架构要求替代。

> 环境补充（2026-09-14）：用户已指定未来部署到与电商运营助手相同的阿里云环境，项目根目录 `/root/myproject`，GitHub 仓库 `https://github.com/feihu0608/AI_Knowledge_Base.git`，向量数据库改为 Milvus。本文后续 pgvector 候选内容已被标准 AI 架构文档 v3.1 的“PostgreSQL 事实源 + Milvus 向量数据面”替代。

> 文档版本：v0.2｜日期：2026-09-14｜状态：企业内部试点候选设计，部署与数据边界待确认后冻结为 v1.0。
>
> 来源：`F:\尚硅谷大模型\项目实战\项目实战.md` 第 812—915 行，即 2.9 节；提交要求见第 2—4 行。本文分析如何开发，不代表系统已经实现、模型已经接通或性能已经验证。

## 1. 先明确这个项目到底做什么

它是一个带组织权限的企业知识平台：管理员上传制度、业务说明等资料，系统把资料拆成可检索片段；员工登录后只能基于自己有权阅读的内容提问；平台把高频问题整理成候选 FAQ，经人工审核后提供快速回答，并通过未解决问题指导补充资料。

完整业务链是：**维护组织身份 → 导入知识 → 配置权限 → 安全问答 → 记录使用 → 挖掘 FAQ/知识缺口 → 人工补充与发布 → 再次问答验证**。

其中 AI 负责语义表示、问答生成、相似问题聚类辅助和候选答案生成；代码负责身份、权限、审批、状态变更、数据写入和指标计算。不能让大模型决定用户是否有权访问资料。

最难的不是聊天页面，而是四件事：所有读取路径遵守同一套权限；导入和更新过程中不出现半成品索引；FAQ 不把私有答案变成公开缓存；用真实业务记录形成可复现的运营闭环。

## 2. 已确认范围与必须追问的问题

### 2.1 从原文可以确认的要求

| 编号 | 原文要求与来源 | 对实现的约束 |
|---|---|---|
| R01 | 三类用户，JWT 登录，部门、角色和按钮权限，2.9.2/2.9.6 | 前端隐藏按钮只是体验优化，后端仍须执行 RBAC 操作鉴权 |
| R02 | PDF/Markdown/Word/TXT，单文件和批量/文件夹导入，2.9.1 | 导入采用异步任务，每个文件有独立处理结果 |
| R03 | 知识单元 CRUD、切片调整、启停和生命周期，2.9.1/2.9.3 | 保存文档版本、切片位置、索引状态，支持重建 |
| R04 | global/department/role/user，默认无访问权限，任一满足可读，2.9.4 | 四维为 OR，不是 AND；数据权限与功能权限分开 |
| R05 | 混合召回后鉴权，只把授权切片放入 Prompt，2.9.4 | 服务端保存鉴权结果；未授权正文绝不发送给生成模型 |
| R06 | 多轮、SSE、Markdown、引用、受限提示，2.9.3 | 历史上下文、引用预览和下载也必须鉴权 |
| R07 | PV/UV、知识量、榜单、Token、延时，2.9.1/2.9.8 | 指标必须有统一事件口径和聚合维度 |
| R08 | 问题聚类、推荐 FAQ、审核发布、缓存，2.9.4 | FAQ 答案必须有来源、可见范围和发布记录 |
| R09 | 未命中/低分问题进入缺口池，转补充任务，2.9.3/2.9.4 | 缺口不能只是列表，要关联补充知识与复验结果 |
| R10 | 审计格式与至少一个端到端场景，2.9.8—2.9.10 | 必须保存召回/放行/拦截列表和演示证据 |

### 2.2 Grill Me 需求发现记录

按 ai-project-builder 技能要求，在确定架构前先做需求追问。当前运行环境没有可调用的原生 Skill 工具，已采用直接提问的等价方式。以下问题没有源文档证据，不能代替用户确认；本文后续只提供可评审候选方案，不通过实现阶段的需求门禁。

| 待确认问题 | 为什么会影响设计 | 本文用于比较的候选方向 |
|---|---|---|
| 已确认用于企业内部试点；期限、参与人数和行业仍待确认 | 决定开发排期、可用性和运维投入 | 以受控企业试点为目标，不等同于正式生产上线 |
| 单组织还是多租户？部门权限是否覆盖下级？用户能否属于多个部门？ | 改变数据模型和鉴权语义 | 单组织、一个直属部门、部门精确匹配；原文仅明确直属部门 |
| 知识管理员管理哪些知识？系统管理员是否自动可读全部正文？ | 决定管理范围和敏感资料暴露边界 | 按知识库配置管理范围；系统管理员不自动获得全部正文 |
| “Word”包括旧版 .doc 吗？扫描 PDF、复杂表格、密码文件怎样处理？ | 决定转换依赖、OCR 成本和失败流程 | 必做 .docx；.doc 单独转换；扫描件进入需 OCR 状态，不能伪装解析成功 |
| 数据来自哪里、是否可发给云端模型？文档数、页数、日增量？ | 决定模型部署、向量容量和保留策略 | 无授权前仅使用自造脱敏示例，不调用真实业务数据 |
| Chat/Embedding/Rerank 用哪家模型？预算和限流？ | 决定接口、维度、成本与延时 | 提供适配层，部署时填写实际模型，不硬编码供应商 |
| “毫秒级”缓存响应测哪个区间？并发和准确性目标？ | 决定验收条件，避免空泛承诺 | 分别测缓存服务、完整 API、浏览器响应 |
| Windows 本机、Linux 主机还是云服务？备份和日志保留多久？ | 决定部署形态、持久卷及恢复策略 | 提供容器化候选拓扑，环境仍待确认 |
| 撤权后历史答案如何处理？受限提示允许透露资料存在吗？ | 决定历史会话重放、流式中断和信息披露边界 | 当前请求重新校验来源；仅显示固定受限提示，不暴露标题和数量 |
| FAQ 频次如何计数、统计窗口多长、谁能审核哪些日志？ | 决定聚类范围、去重和运营权限 | 权限范围内聚类，按有效请求与独立人数分开统计 |

**当前需求基线：**用户已确认用途为企业内部试点；原文功能属于已确认的目标范围；技术选型、性能目标和部署条件属于建议或待确认。没有把候选值写成用户承诺。确认后给 R01—R10 补充负责人、优先级、门槛和对应测试，形成 v1.0。

### 2.3 交付范围分层

首个开发里程碑可先完成“登录 → 导入文本文件 → 配权限 → 混合检索 → 真实模型回答 → 引用 → 审计”，用于验证最危险的权限链路。但它不是整个项目的完成标准。

原文完整验收仍包含四类文件、批量导入、组织维护、完整四维权限、FAQ 挖掘审批缓存、缺口处理、看板和演示。OCR、大规模分布式检索、外部网盘同步、SSO、多租户和知识图谱可以作为待确认扩展，不能用未实现扩展替代必做功能。复杂表格和扫描件的支持边界必须在最终验收前明确。

## 3. 建议技术架构与选择理由

### 3.1 候选方案：模块化单体 + 独立任务进程

| 层次 | 建议技术 | 负责什么及选择理由 |
|---|---|---|
| Web | Vue 3 + TypeScript + Vite + Element Plus + ECharts | 组织树、权限弹窗、上传队列、聊天工作台和看板；若团队熟悉 React 可替换 |
| API | Python FastAPI + Pydantic + SQLAlchemy + Alembic | 身份鉴权、领域操作、SSE、模型适配与数据库迁移 |
| 事实数据库 | PostgreSQL | 用户、ACL、文档、版本、任务、会话、FAQ、审计的权威数据源 |
| 检索 | pgvector + 中文关键词索引 | 初期同库方便关联 ACL 和事务；数据规模实际压测后再判断是否分离 |
| 队列与缓存 | Redis + Celery worker/beat | 异步解析、向量化、聚类、聚合任务；缓存只做加速，不做权限事实源 |
| 原始文件 | 本地持久目录，封装存储接口；后续可换 S3 兼容对象存储 | 文件必须受 API 授权访问，不能直接暴露磁盘目录 |
| 模型层 | Chat/Embedding/Rerank 独立接口 | 允许不同供应商和自部署；真实模式失败就返回明确状态，不切成假答案 |

这里的“中文关键词索引”不是直接使用 PostgreSQL 默认英文全文检索配置。首期可用固定版本中文分词器，将正文与查询用同一规则分词后以 simple 配置建立 tsvector/GIN 索引；维护词典版本并评估领域术语。若分词和规模无法满足目标，再采用有中文分析器的 OpenSearch/Elasticsearch，承担额外同步成本。

不需要一开始拆成六个微服务。原文六类后端服务可先实现为六个代码模块，运行 API 和 worker 两种进程。也不需要多 Agent：核心流程步骤固定，普通函数和任务状态机比自主 Agent 更容易审计。LangChain 仅作可替换集成辅助；LangGraph 可在以后出现复杂分支和人工中断时评估。

### 3.2 组件关系

```text
浏览器：组织配置 / 知识管理 / AI 对话 / FAQ 与缺口 / 看板
                        |
                  反向代理 / API
                        |
       JWT 身份验证 + 服务端 RBAC + 数据 ACL
                        |
      组织模块 ─ 知识模块 ─ 问答模块 ─ 运营模块
                        |
        PostgreSQL + pgvector（权威事实与索引）
           |                |                |
      私有文件存储      Redis 队列/缓存     模型适配层
                            |           Chat/Embedding/Rerank
                      独立 worker
                解析 / 索引 / 聚类 / 指标聚合
```

网络上只向用户暴露 Web/API；数据库、Redis、worker 和文件存储均保持内部访问。模型提供商是外部信任边界，文档、问题和向量化内容能否离开内网必须先确认。

### 3.3 代码目录建议（尚未创建的目标结构）

```text
frontend/src/{pages,components,api,stores}
backend/app/
  auth/ organization/ knowledge/ permissions/
  retrieval/ chat/ models/ faq/ gaps/ analytics/ audit/
  workers/ storage/ core/
backend/migrations/
tests/{unit,integration,e2e,security,evals}/
data/demo/  deploy/  docs/  evidence/
```

模块只能通过明确领域接口修改其他模块的数据，尤其不能由聊天模块绕过权限模块直接读取文件。

## 4. 四维权限：这个项目的核心

### 4.1 功能权限和数据权限同时成立

功能权限回答“可不可以调用这个操作”，例如 `knowledge.upload`、`knowledge.acl.update`、`faq.publish`；数据权限回答“这个对象的内容能否被当前用户读取”。

```text
允许读取 = 用户有效且已登录
       AND 拥有本接口所需功能权限
       AND 知识启用且当前版本可用
       AND（global
             OR 用户直属部门匹配 department
             OR 用户任一角色匹配 role
             OR 当前用户 ID 匹配 user）
```

没有 ACL 的知识默认拒绝普通读取。不要把“上传者”偷偷作为第五条 OR 规则；上传者如需阅读应显式写入个人 ACL。管理者查看未发布文档走独立的受审计管理预览接口。global 建议解释为“当前组织全部已登录成员”，不是互联网匿名公开，这一点应最终确认。

部门树用于组织维护不等于自动权限继承。若后续决定父部门授权覆盖子部门，必须使用受版本管理的祖先关系查询，并把部门移动纳入权限失效测试。JWT 不长期缓存可信角色列表：服务端根据 user_id 读取最新用户状态和 authz_version，以便禁用和撤权生效。

### 4.2 召回后的严格裁剪与召回不足

按原文先混合检索候选，再执行 ACL。候选中未授权内容只保留在可信服务端，生成 Prompt、外部 reranker、面向用户的 SSE、调试响应与引用列表均不携带未授权正文。

若先取 20 个候选再过滤只剩 1 个，应扩大召回或继续扫描到候选上限，不能把“被过滤后太少”直接当成没有知识。可用 ACL 条件预筛降低成本，但原文要求的受限提示需要独立的内部判断；不能在外部模型调用中检查受限正文。

仅存在未授权候选时，不让模型靠常识猜测内部制度。给出固定提示“部分参考资料因权限受限无法展示”，或者固定无权限说明。该提示按原文会透露存在相关受限资料，这是源要求的取舍；不额外透露文件名、摘要、作者、数量或片段。

### 4.3 必须覆盖的权限入口

| 入口 | 必须执行的控制 |
|---|---|
| 搜索/问答 | 当前用户身份 + 功能权限 + 候选知识 ACL |
| 标题联想/推荐提问 | 候选内容可见性过滤，防止标题侧漏 |
| 引用卡片/文档预览/下载 | 每次按 doc_id/version_id 再鉴权，禁止使用可绕过接口的永久静态链接 |
| 历史会话/导出 | 校验会话归属，并对模型回复的全部知识来源重新鉴权 |
| 多轮 Prompt | 只回放仍有权访问且未失效的历史知识回复；新问题仍重新检索 |
| FAQ 缓存 | 校验 FAQ ACL 和全部依赖知识的当前 ACL、有效版本 |
| FAQ 审核/日志聚类 | 操作者具有管理范围；后台任务按合法数据处理范围读取日志 |
| 看板/审计 | 聚合指标、问题原文、拦截 ID 分权；普通用户不得读取内部召回列表 |

### 4.4 撤权和缓存一致性

在数据库事务中更新 ACL 和 acl_version，同时插入 outbox 事件；worker 发布失效通知、刷新缓存、标记关联 FAQ 待重验。即使通知失败，读取前也以数据库的当前版本和 ACL 为准，Redis 不能让旧权限继续放行。

撤权生效边界建议定义为“ACL 事务提交后，新请求与新片段发送前执行当前权限检查”。流式会话保存来源集合，发送批次前检测版本变化，失去任一所用来源权限即终止生成并标记回答不可重放。已经显示或下载的数据不能技术性收回；这个边界必须写进验收。

## 5. 数据模型与状态机

### 5.1 核心表建议

| 表 | 关键字段 | 关键约束 |
|---|---|---|
| users/departments/roles/user_roles | user_id、status、department_id、parent_id、authz_version | 部门树禁止环；禁用用户不能继续发起问答 |
| permissions/role_permissions | permission_code、role_id | 权限码唯一，所有敏感 API 使用同一校验入口 |
| knowledge_bases/kb_managers | kb_id、manager_id、管理动作范围 | 管理范围独立于普通内容读取权限 |
| documents | doc_id、kb_id、title、enabled、active_version_id、acl_version | 生命周期与正在处理的版本分离 |
| document_acl | doc_id、entity_type、entity_id | global 使用固定哨兵并禁止任意 ID；三类主体验证存在；组合唯一 |
| document_versions | version_id、doc_id、sha256、parser_version、status、storage_key | 保留原文和解析配置；hash 用于识别同库重复输入 |
| chunks | chunk_id、version_id、ordinal、text、page、heading_path、offsets | 引用稳定定位；与文档版本绑定 |
| chunk_embeddings | chunk_id、model_id、dimension、embedding、index_version | 模型或维度改变必须新建兼容索引，不能混查 |
| import_batches/import_jobs | batch_id、file_id、stage、completed_units、total_units、attempt、error_code | 每文件独立终态；进度来自实际任务计数 |
| conversations/messages | conversation_id、owner_id、message_id、status、content、request_id | 会话归属校验；中断回复不当作完整答案 |
| answer_sources | message_id、chunk_id、version_id、acl_version | 历史重放和撤权判断的依据 |
| qa_events/model_calls | request_id、user_id、候选/放行/拦截 IDs、Token、阶段耗时 | request_id 去重；原始审计受限访问 |
| faq_clusters/faq_candidates | cluster_id、time_window、count、source_ids、proposed_answer、score | 保存算法/模型/阈值版本和生成证据 |
| faq_items/faq_reviews/faq_sources | faq_id、version、status、ACL、reviewer、decision、依赖来源 | 已发布版本不可原地无审计覆盖 |
| knowledge_gaps/knowledge_tasks | gap_id、reason、count、department_id、status、resolved_doc_id | 原问题保留访问范围；关联验证记录 |
| outbox_events/audit_logs | event_id、aggregate_id、version、actor、action、time | 事件可重投，审计不能随业务删除被抹去 |

“知识单元”建议在管理界面解释为一篇逻辑文档或一条 FAQ；chunk 是其内部检索片段，不单独扩大 ACL。看板分别展示逻辑文档数、已发布 FAQ 数和 chunk 数，避免重复计算。若业务需要片段单独授权，须补充 chunk ACL 及与文档 ACL 的组合规则，不能自行假设。

### 5.2 状态机

```text
导入任务：queued → parsing → chunking → embedding → indexing → succeeded
                                  任一阶段 → failed / cancelled
文档版本：draft → processing → ready → active → superseded
知识启用：enabled ↔ disabled；deleted 采用可追踪删除状态
问答：accepted → retrieving → generating → completed
                            ↘ denied / no_evidence / failed / cancelled
FAQ：draft → submitted → approved → published → invalidated / archived
                  ↘ rejected → draft
知识缺口：open → assigned → supplementing → verifying → resolved
                                                    ↘ open（复验失败）
```

状态转换由代码验证当前状态与操作者权限；审核通过和发布可由一次 UI 操作触发，但仍分别保留审核结果、发布版本和 outbox 事件。缓存写入失败时显示“已发布，缓存同步待重试”，不能伪称缓存已生效。

## 6. 从用户操作到结果的完整链路

### 6.1 文档导入与更新

1. 用户进入上传抽屉，前端校验扩展名、大小和数量；文件夹导入读取浏览器提供的相对路径，用作展示分类而不是磁盘路径。
2. `POST /api/import-batches` 验证上传权限和知识库管理范围，检查真实文件类型、限额和路径安全，将原文件存入私有存储。
3. 数据库写入文档草稿、版本、逐文件任务与 outbox，再返回 batch_id；前端轮询任务或订阅进度事件。
4. worker 按格式解析：PDF 提取页码与文本，DOCX 提取段落和表格，Markdown 保留标题，TXT 识别编码；解析失败给出具体原因。
5. 清洗页眉页脚、重复空白但保留结构；按标题与段落切块，再按 tokenizer 控制长度。建议起始 500—800 tokens、重叠 80—120 tokens，仅作为调优起点。
6. 每片记录页码、标题路径和字符位置。表格按行或逻辑区域处理并重复表头，避免把字段与数值拆散。
7. 批量调用 Embedding，记录模型、维度和失败批次。全部向量及关键词索引完成后校验 chunk 数量与索引覆盖率。
8. 在事务内激活新版本。旧版本在切换前继续服务，失败的新版本不能污染线上索引；初次上传则保持不可检索直到准备完成。
9. 管理员配置四维 ACL 或显式个人权限；默认无访问权限。前端刷新后仍能看到真实版本、处理结果和授权状态。

删除/禁用先阻止查询，再异步清理索引和原文件，并使相关 FAQ 失效。编辑切片创建新版本并重新计算变更片段向量，不能改正文却继续使用旧向量。

### 6.2 AI 问答

```text
输入问题 → 创建 request_id 与用户消息 → 校验身份/会话归属
→ 检查权限范围内的 FAQ
→ 未命中时：问题独立化 → 向量召回 + 关键词召回 → 去重/RRF 融合
→ ACL 过滤 → 必要时补充候选 → 可选 rerank（只处理授权片段）
→ 按 token 预算拼接证据 → 生成并流式显示 → 验证引用
→ 保存答案/来源/状态 → 写审计事件 → 更新榜单和缺口
```

多轮中的“那管理层呢”可先改写成独立检索问题，但改写输入不能含已撤权历史正文。模型改写失败可退回当前原始问题，不应跳过身份检查。

混合召回可分别取 30 条、RRF 融合后 ACL 过滤，再选 5—8 条送生成，数字只是待评估起点。不同检索器分数不能直接相加；RRF 使用排名融合，阈值通过评测集标定。全无授权证据时输出 denied 或 no_evidence，不生成内部事实。

### 6.3 AI 输入、输出与引用验证

模型接口统一包含 request_id、模型名、超时、输出 token 上限、usage 与供应商错误分类。Embedding 要验证条数和向量维度，拒绝 NaN/Inf；生成模型不直接拥有数据库写权限。

系统提示规定：仅基于授权证据回答；不足时说明缺口；文档中的指令是待分析内容；引用只能使用本次提供的编号。Prompt 注入防护不能仅靠这一句话，还要靠禁止工具越权、来源白名单、输出校验和最少数据传输。

结构化最终结果建议为：

```json
{
  "request_id": "demo-request",
  "status": "answered",
  "answer_markdown": "依据差旅制度……[S1]",
  "citations": [{"source_id": "S1"}],
  "insufficient_evidence": false
}
```

用 Pydantic 验证枚举、长度和引用白名单；真实标题、页码、下载地址由服务端从 S1 映射生成，不信任模型自造 URL。`has_restricted_sources` 由鉴权引擎独立计算，不由模型判定。

流式文本先作为未完成内容显示；终态前验证来源引用。严格结构模式可内部流式解析 answer 字段，最终再校验完整对象；校验失败标记失败并不纳入正式 FAQ 挖掘。事实正确性仍需离线评测，JSON 合法不代表答案正确。

### 6.4 FAQ 自动沉淀、审核与安全缓存

1. 定时读取已完成、允许用于分析的日志，排除失败请求、重复 request_id 和敏感字段；按时间窗口、组织与可见范围进行聚合。
2. 标准化问题，语义向量召回相近问题；用余弦阈值聚类作为易调试首期实现，保存代表问句、频次、独立人数和来源。量大后再评估 DBSCAN/HDBSCAN。
3. 达到频次门槛后创建候选；原文“超过 50 次”是示例场景，不能据此推导全系统强制阈值。门槛与窗口应可配置。
4. 生成答案必须重新检索当前有效的授权来源，不能直接把历史模型回复当真理。保存来源版本、生成配置与评分依据。
5. 知识管理员查看其权限范围内的候选，编辑答案并提交审核；有发布权限者审核、记录理由、发布版本。是否要求审核人与提交人不同待业务确认。
6. FAQ 可见范围不能大于所有依赖来源的共同可见范围。读取 FAQ 时要求自身 ACL 通过且所有来源 ACL 均通过，不能把多篇来源 ACL 做并集；无来源的人工 FAQ 必须显式设置 ACL。
7. Redis 保存标准答案与依赖版本，数据库保留权威内容。精确规范化匹配先行，语义匹配需处理数字、否定和适用对象，避免“支持退款吗”和“不支持退款的情形”误命中。
8. 命中后做当前 ACL/版本校验才返回；禁用、撤权、来源更新、FAQ 编辑都触发失效。缓存不可用时回退到数据库验证或正常 RAG，不返回未经鉴权的旧值。

缓存键至少包含组织边界（若多租户）、规范化问题摘要、FAQ 版本、权限版本/范围指纹、语义模型版本。即使使用指纹，每次仍校验当前来源。不能只用 `question → answer` 作为全员共享缓存。

“毫秒级”应理解为可以单独测量的缓存分支目标。语义匹配若需要网络 Embedding，延时就包含该调用，不能拿 Redis GET 的耗时冒充完整问答时间。

### 6.5 知识缺口与运营闭环

将问题区分为 `no_match`、`low_relevance`、`insufficient_evidence`、`access_denied`、`provider_error`。前三类可进入知识建设缺口；权限拒绝进入访问相关诊断，模型服务失败进入运行告警，避免让管理员针对“无权查看”重复上传已有资料。

缺口池记录问题、部门、频次、最高相似度、检索器/模型版本及来源状态，按允许访问的范围聚合。管理员创建补充任务、指派、导入新资料，并用原问题在原测试身份下再次提问。只有能命中正确有效来源且人工验证通过，才标记 resolved。

## 7. API 与事件契约

下表是待实现接口设计，不是已经可访问的地址。所有修改记录 actor_id 和 request_id，列表接口分页且根据管理范围过滤。

| 接口 | 核心输入与输出 | 权限/一致性要求 |
|---|---|---|
| POST /api/auth/login | 账号密码 → access token 与用户摘要 | 密码哈希、失败限速、禁用状态校验 |
| GET /api/me | 当前组织身份、权限码 | 不接受客户端伪造部门和角色 |
| /api/users、/departments、/roles | 标准 CRUD、部门 parent_id、角色权限码 | 系统管理权限，防部门循环和角色提权 |
| POST /api/import-batches | 文件、kb_id、幂等键 → batch_id、jobs | 202 Accepted；逐文件检查与持久化 |
| GET /api/import-batches/{id} | 每文件 stage、实际计数、错误码 | 上传者或有范围权限的管理员 |
| POST /api/import-jobs/{id}/cancel | 取消申请 → cancel_requested | worker 在安全检查点执行，不假装立即终止 |
| /api/documents、/{id}/chunks | 列表、编辑、启停、删除 | 管理 RBAC + 管理范围；读正文走 ACL |
| PUT /api/documents/{id}/acl | entities、expected_acl_version | 乐观锁冲突返回 409，事务写 outbox |
| GET /api/sources/{id}、/{id}/download | 引用定位、文件内容 | 每次鉴权；无访问权限不泄露标题 |
| POST /api/conversations/{id}/messages | question、client_request_id → SSE | 会话归属与 AI 使用权限；重复请求不重复计费 |
| GET /api/requests/{id} | 当前状态、持久化内容、终态 | 重连恢复读取，不重新创建模型请求 |
| /api/faq-candidates/{id}/submit、/review | 编辑后提交、决定与理由 | 状态校验、审核角色与管理范围 |
| POST /api/faqs/{id}/publish | expected_version → published_version | 发布事务与缓存同步状态分开 |
| POST /api/gaps/{id}/tasks | 负责人、分类 → task_id | 缺口可见范围与指派权限 |
| GET /api/analytics/summary | 时间范围、粒度 → 指标与统计口径 | 参数化聚合、运营权限 |
| GET /api/audits | request_id、筛选 → 受限审计记录 | 普通用户不能读取拦截列表 |

SSE 事件统一携带 event_id、request_id、type、data；建议类型：`accepted`、`retrieval_status`、`restriction_notice`、`delta`、`citation`、`done`、`error`。POST 流可通过 fetch + ReadableStream 实现，原生 EventSource 不便携带 Authorization 请求头和 POST body。不要把 JWT 放 URL 查询串；反向代理关闭流式缓冲。

错误体统一 `{code, message, request_id, retryable}`。例：401 身份失效，403 无操作权限，404 对不可见对象统一隐藏，409 版本冲突，413 文件超限，422 格式不支持，429 限流，503 模型不可用。受限知识提示属于受控业务结果，不附内部堆栈。

后台事件建议：`DocumentVersionReady`、`DocumentAclChanged`、`DocumentDisabled`、`AnswerCompleted`、`FaqPublished`。事件包含 event_id、聚合对象 ID、version、occurred_at；消费者按 event_id 去重，不在队列中携带大段敏感正文。

## 8. 异步、重试和可靠性

| 问题 | 推荐处理 |
|---|---|
| 上传成功但队列没收到 | 数据库任务与 outbox 同事务；投递器持续重试；定期扫描未投递事件 |
| worker 崩溃 | 心跳与租约判断超时；从已持久化阶段恢复；每阶段幂等 |
| 同一文件重复导入 | 同 kb、hash、解析配置识别已有版本；不同 kb 或独立 ACL 不盲目合并对象 |
| Embedding 中途失败 | 以 version_id + chunk_id + model_id 去重，只补失败批次 |
| 429/临时 5xx | 有上限的指数退避 + jitter；鉴权/参数错误不重试；超出预算停止 |
| 生成已输出后断线 | 持久化部分内容和状态，前端可恢复读取；不自动重发整次有计费请求 |
| 超时与取消 | 连接、模型首 token、总时长分别配置；取消检查点阻止新写入/新调用，记录已发生费用 |
| Redis 失效 | 权限回查数据库；缓存旁路；任务积压可见，不能靠缓存决定授权 |
| worker 重复消费 | 唯一约束 + 状态条件更新，防止重复发布和重复统计 |

进度条由阶段和完成数量驱动，例如“已向量化 18/24 个切片”；不知道总量时显示阶段中的不定进度，不编造 0—100% 动画。批量结果允许部分成功，用户能单独重试失败文件。

## 9. 运营指标与评测方法

### 9.1 指标口径先固定

| 指标 | 建议定义 |
|---|---|
| 工作台 PV | 工作台页面访问事件次数；与问答请求量单独展示 |
| 问答 UV | 统计时间窗内发起有效问答的去重 user_id；工作台访问 UV 可另列 |
| 总提问量 | 按 request_id 去重的已接受问题数量，并列成功/失败/拒绝 |
| 知识单元总量 | 非删除文档与发布 FAQ 分项展示，启用/处理中另列 |
| 热门知识 | 完成答案中的引用次数，按 request_id + doc_id 去重 |
| 高频问题 | 规范化或聚类簇的有效问题数，同时显示人数、窗口和算法版本 |
| FAQ 直接命中率 | 成功由 FAQ 返回的请求数 / 进入 FAQ 检查的合格请求数 |
| Token | 按模型调用记录真实 usage，分 Chat/Embedding/改写/FAQ 挖掘；无 usage 为 unknown，不能写 0 |
| 延时 | 分别记录 FAQ、检索、首 token、生成总时长与整体 P50/P95；均值不是分布 |
| 覆盖率 | 固定评测问题集上有正确且授权证据的问题数 / 有效评测问题数；线上可回答率另算 |

时间范围使用确定的业务时区，数据库时间存 UTC。按 request_id 去重聚合，问答流水先持久化，再异步聚合，避免日志服务故障导致统计长期丢失。供应商 usage 与账单仍可能有差异，成本只在价格已确认且 usage 完整时估算。

### 9.2 评测集和指标

推荐先整理 60—100 条带身份、预期来源、标准结论和拒绝预期的问题，覆盖原问、改写、多轮、部门隔离、角色跨部门、个人专属、空 ACL、撤权、无答案、相似 FAQ 误命中和文档注入。开发调参与最终测试题分离，禁止用同一批题既调阈值又宣称泛化效果。

质量指标包括授权证据 Recall@K、引用正确率、答案依据一致性、FAQ 精确率、拒绝正确率、越权泄漏次数和知识缺口分类准确率。相似度与 reranker 分数不是“事实置信度”，阈值需要由标注集和误判成本决定。

候选验收门槛可设：测试集中越权泄漏 0 次；支持四种独立授权和混合 OR；引用正确率 ≥95%；授权证据 Recall@5 ≥90%。这些是建议，尚未取得用户确认，也不表示已达成。零泄漏样例通过不等于证明所有未知攻击安全。

延时必须附硬件、文档/chunk 数、并发、冷/热缓存、模型和网络条件。可以把热缓存完整 API P95 <200ms、问答首 token P95 <3s 作为讨论起点，但不得承诺云模型一定达到。成本预算按“请求量 × 各类模型调用 token × 对应单价 + 重试 + 后台挖掘”估算，本文不编造价格。

## 10. 需求到测试的对应关系

所有下列测试当前状态均为 **未执行，待开发**。预期结果不是测试结果。

| 测试 | 对应需求/组件 | 前置与操作 | 预期及证据 |
|---|---|---|---|
| T01 | R01 身份/RBAC | 普通用户登录，调用隐藏的用户管理 API；禁用后再请求 | 后端拒绝；保存角色截图与 API 结果 |
| T02 | R02 导入 | 通过界面分别上传四类支持格式，再批量含一个坏文件 | 实际解析、chunk 与索引数一致；坏文件独立失败；保存任务记录 |
| T03 | R03 版本生命周期 | 编辑切片、禁用、重启后查询、删除并打开旧引用 | 新版本正确生效；禁用/删除不可检索；旧入口受控 |
| T04 | R04 ACL | 各设 global、直属部门、角色、个人及空 ACL，多身份读取 | 任一 OR 条件满足可读，空 ACL 拒绝；参数化测试矩阵 |
| T05 | R05 检索隔离 | 普通业务人员询问高管薪酬，抓取测试模型适配器输入 | 未授权正文不进 Prompt/外部 reranker；只有固定受限提示 |
| T06 | R06 多轮/引用 | A 用户访问 B 会话；撤权后回放、下载和多轮续问 | 会话隔离、来源重新校验，历史敏感答案不能重新发送 |
| T07 | R06 SSE | 模型超时、断网重连、重复 client_request_id | 明确失败/恢复状态，无重复创建和伪成功回复 |
| T08 | R07 指标 | 用可精确计数的事件集含重复 ID、失败、FAQ 命中 | 看板与独立 SQL 汇总一致，unknown usage 不冒充 0 |
| T09 | R08 FAQ | 多种相似提问产生候选，越权审批后再由合法人员审核发布 | 越权拒绝；合法审核有审计；缓存可查真实发布版本 |
| T10 | R08 缓存权限 | HR 命中薪酬 FAQ，业务用户问同句；撤销来源 ACL 再问 | 缓存不越权；撤权提交后的请求不能读取旧答案 |
| T11 | R09 缺口 | 用清关延误无资料问题触发缺口，转任务、补文档、复问 | 原问题命中补充来源并关闭缺口；权限拒绝不误当内容缺口 |
| T12 | R02/R03 异步恢复 | worker 中断、任务重投、索引完成前失败 | 恢复不重复，旧有效版本仍服务，进度是真实计数 |
| T13 | R05 AI 质量 | 对固定独立测试集执行真实模型问答与检索评测 | 保存模型/索引/数据版本、逐题结果及汇总，不只截图 |
| T14 | R10 端到端 | 通过浏览器完成下节业务场景，刷新/重启后再查看 | 数据与引用持久，审计可复现；附截图或录像 |
| T15 | R04/R08 并发 | FAQ 命中与撤权并发、流式生成中撤权、缓存通知丢失 | 按约定生效边界停止后续输出，数据库校验阻断旧缓存 |

单元测试覆盖 ACL 真值表和状态机；集成测试使用真实数据库/Redis 和故障注入；真实模型冒烟验证 Chat、Embedding，启用 Rerank 时也验证对应服务；浏览器 E2E 验证真实持久化链路。Mock 可用于可控故障和 Prompt 捕获，但不能代替真实模型验收。

## 11. 演示数据与答辩路线

建议准备一个虚构企业，部门含业务、财务、人力资源、客服；用户至少包括业务员工、HR 员工、跨部门管理层角色用户、知识管理员、系统管理员。账号在本地初始化，密码由环境或生成脚本提供，文档不固化真实密码。

| 示例知识 | ACL | 演示价值 |
|---|---|---|
| 差旅报销标准 | global | 全员可用，检验引用页码与版本 |
| 高管薪酬与股权细则（虚构数据） | HR 部门 OR 管理层角色 | 验证部门与角色独立放行、普通员工受限 |
| 员工个人培训计划 | 指定 user | 验证个人专属 |
| 未发布财务说明 | 空 ACL | 默认拒绝，不能因为已入向量库而暴露 |
| 生鲜破损退款流程 | 客服部门 | FAQ 自动挖掘、审核、同义问匹配和权限缓存 |
| 海外直邮清关延误说明 | 初始不存在，后续补充 | 生成缺口、转任务、补充与复验 |

演示 A：知识管理员导入差旅与薪酬资料并设置权限；普通用户能回答差旅问题，问薪酬只显示安全提示；HR 与管理层角色分别获得有来源的薪酬答案；撤销管理层角色再访问 FAQ、历史和引用，证明其他入口也失效。

演示 B：通过问答 API 写入明确标注的演示请求，产生多种“生鲜破损退款”问法；聚类任务生成候选，管理员编辑审核发布；再次提问命中 FAQ，展示该请求没有新增生成调用；清关延误问题形成缺口，补文档后重新提问并关闭任务。

为缩短现场演示，可以把挖掘窗口/频次阈值配置为演示值，但必须标注，不能把直接造好的候选列表称为自动挖掘证据。原文只要求至少一个典型场景；建议两条都演示，以覆盖权限和沉淀两种核心能力。

## 12. 按纵向功能链开发的顺序

以下按依赖顺序推进。企业试点暂不承诺工期。需先确认身份接入、数据审批、部署资源和验收窗口，再按人员投入估算；阶段之间以证据过关，不能只按日历推进。

| 阶段 | 实现内容 | 阶段完成证据 |
|---|---|---|
| M0 需求基线 | 回答关键问题，冻结术语、权限和验收值，准备标注题 | v1.0 基线与追踪矩阵 |
| M1 组织与权限 | 数据迁移、登录、组织角色、RBAC、ACL 引擎 | 多身份 API 和界面权限测试 |
| M2 导入到可检索 | 先 MD/TXT，再 DOCX/PDF、批量、任务进度、版本切换 | 上传后真实片段/向量可查，失败可恢复 |
| M3 安全 AI 问答 | 混合检索、权限过滤、真实模型、SSE、引用与历史 | 多角色真实回答、无授权内容的 Prompt 证据 |
| M4 FAQ 与缺口 | 日志聚类、候选审批、缓存、来源失效、补充复验 | 审核闭环、缓存无重复模型调用、缺口关闭 |
| M5 看板与可靠性 | 指标聚合、审计、并发撤权、恢复与备份 | SQL 对账、故障注入、重启与恢复记录 |
| M6 完整验收 | 独立评测、浏览器演示、干净环境启动、提交包 | 测试报告、截图/录像、部署与恢复说明 |

不要到最后才补权限，ACL 的表结构与读取入口必须在 M1 确立；也不要先把所有页面画完再接接口，每阶段都应该能从界面操作走到持久化结果。

## 13. 部署、配置与维护设计

### 13.1 当前环境事实与边界

本次检查时目标目录为空，项目实战根目录不是 Git 仓库，没有现成平台代码可审查；限定检查的目录及其父目录未发现 AGENTS.md。当前 shell 的 Get-Command 检查没有返回 python/node/docker 的可用路径，这只说明当前命令发现结果，不能断言电脑完全未安装它们。本次未探测端口、启动服务、安装依赖或调用外部模型。

### 13.2 候选部署拓扑

开发可用 Windows + WSL2/Docker Desktop，验收可在 Linux 单机 Docker Compose 运行 web、api、worker、scheduler、postgres、redis。小规模试点可把文件作为独立持久卷；后续再替换对象存储。Compose 的存在不能代替真正的启动验证。

部署配置应包括数据库连接、Redis 连接、JWT 密钥、文件根目录、模型 base URL/model ID/key、Embedding 维度、超时/重试、文件限额、聚类窗口、缓存阈值和审计保留期。提供 `.env.example` 占位模板，真实密钥放受保护且被 Git 忽略的 `.env` 或密钥服务，管理界面仅显示是否已配置，不能回传完整密钥。

当前没有代码、依赖锁或 Compose 文件，因此没有可以诚实宣称已验证的安装/启动命令。实施后交付必须包含可复制的依赖安装、迁移、初始化、启动、停止、重启、备份、恢复与回滚命令，并从干净环境逐条执行。此分析不提供会让人误以为已经能运行的命令。

### 13.3 运维要点

- 观测：request_id/job_id 贯穿 API、队列、模型与审计；跟踪解析失败率、队列积压、模型 429、检索延时、缓存失效滞后和无证据率。日志默认不打印正文或密钥。
- 健康检查：区分进程存活与数据库/队列可用；外部模型故障应显示降级原因，不能通过无关健康接口掩盖。
- 备份：同步保留数据库、原文件及其版本清单；向量可重建但成本高，保存模型/分块配置。Redis 缓存可重建，不能是唯一事实存储。
- 恢复：在隔离环境恢复数据库和文件，验证随机引用、ACL 和文档 hash，再恢复任务调度，防止对旧 outbox 重复触发费用。
- 更新：迁移先在副本验证；应用与数据库采用兼容迁移，保留旧版本镜像。向量模型变更并行构建新索引、评测后切换，失败切回旧索引。
- 保留和清理：会话与审计保留期限待确认；清理同时覆盖原文件、解析文本、向量、缓存和备份生命周期，审计保留最小必要元数据。

## 14. 高风险决策记录

| 决策 | 本文建议 | 代价与重新评估条件 |
|---|---|---|
| ADR-01 架构粒度 | 模块化单体 + worker | 模块隔离依靠工程约束；只有实际容量/团队边界需要时再拆服务 |
| ADR-02 检索存储 | PostgreSQL/pgvector 同库存储 | 中文关键词和 ANN 过滤要压测；规模或质量不达标再迁移 |
| ADR-03 鉴权事实源 | 数据库当前 ACL 权威，缓存加速 | 增加查询成本，可批量校验；换来清晰的撤权边界 |
| ADR-04 FAQ 授权 | 自身 ACL 与所有依赖来源共同约束 | 多来源 FAQ 可能可见范围较窄，宁可重新编写公开 FAQ，也不默认扩权 |
| ADR-05 文档更新 | 新版本构建完成再激活 | 临时占用更多存储，换取查询期间版本一致性 |
| ADR-06 受限提示 | 遵循原文固定提示但不显示具体资料 | 仍透露相关受限知识存在；企业上线前需确认政策 |
| ADR-07 模型方案 | Chat/Embedding/Rerank 解耦 | 多一层适配工作，避免模型替换迫使业务重写 |

## 15. 最终交付检查表与本次交付状态

真正完成项目时，应交付前后端源代码（不带依赖目录）、数据库迁移、样例数据与初始化方式、依赖锁、环境变量说明、启动停止和备份恢复文档、逐条需求测试报告、真实模型调用证据、端到端截图/录像及已知限制。按源文档总要求，最终提交文件夹使用“组名_姓名_项目_行业”命名；本次分析按用户指定放在“知识库管理平台”目录。

本次完成的是源要求拆解、候选架构、数据与权限设计、主链路、接口、异常处理、开发路线、测试矩阵和风险分析。未实施业务代码，未冻结待确认需求，未验证模型效果、性能、部署和浏览器业务链。下一步应先确认第 2.2 节的关键选择，再按 M1—M6 开发；不能把本文的设计预期当作已完成系统。


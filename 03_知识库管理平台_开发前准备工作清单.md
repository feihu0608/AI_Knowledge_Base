# 知识库管理平台开发前准备工作清单

版本：v1.0｜日期：2026-09-14｜状态：只记录，未执行

## 1. 当前已经具备或确认

| 项目 | 当前记录 | 验证状态 |
|---|---|---|
| 业务需求 | 需求规格 v1.1，功能范围已明确 | 已形成文档，未实现 |
| AI 架构 | LangGraph + Agent Harness + 多 Agent + Celery | 已形成文档，未实现 |
| 云环境 | 与电商运营助手相同的阿里云 ECS 环境 | 来自用户和本地旧项目文档，本次未登录复核 |
| 服务器根目录 | `/root/myproject` 已创建 | 用户确认，本次未远程验证 |
| GitHub 仓库 | `https://github.com/feihu0608/AI_Knowledge_Base.git` | 用户确认已创建，本次未访问 |
| 关系数据库 | PostgreSQL | 已定架构，实例未建立 |
| 向量数据库 | Milvus | 用户已定，部署形态未定 |
| 缓存/队列 | Redis + Celery | 沿用环境，未配置 |
| 文档解析 | MinerU API | 服务端点、账户和限制未确认 |
| 模型 | 硅基流动 DeepSeek-V3、bge-large-zh-v1.5、bge-reranker-v2-m3 | 名称已定，可用性未验证 |
| MCP | 魔搭 API/MCP | 具体服务和工具清单未定 |
| 数据 | 自造两个租户的虚构数据 | 设计已定，数据文件未生成 |

## 2. 开工前必须完成的 P0 决策

### P0-1 Milvus 部署形态

三选一并记录理由：

1. 独立阿里云 ECS 自建 Milvus Standalone：隔离清晰，需要独立维护、备份、监控和升级。
2. 经企业确认的托管 Milvus 服务：运维较少，需要确认网络、费用、地域和数据边界。
3. 与应用同一 ECS：只适合资源验证通过的小规模演示。电商助手记录的 2 核 4 GiB 不应直接采用此方案。

需要拿到：当前 ECS CPU/RAM/磁盘/Swap、Docker 版本、预计 chunk 数、向量维度、日新增、并发查询、可接受恢复时间。没有这些数据，不能定 index 参数和容量。

### P0-2 数据外发审批

分别确认以下数据能否发送：

- 原文件到 MinerU。
- chunk 正文到硅基流动 Embedding。
- 问题和候选正文到 Rerank。
- 文档摘要/样本和问答证据到 DeepSeek-V3。
- 问题和必要上下文到魔搭 MCP。

每项记录允许的数据分类、脱敏规则、地域、日志留存和禁止字段。未批准时只用虚构数据。

### P0-3 外部服务清单

准备但不要在文档或聊天中粘贴密钥：MinerU 服务类型和文档、硅基流动账户/三个模型权限、魔搭具体 API/MCP server/tool、各自限额和预算、负责人。实施时只做最小授权冒烟测试。

### P0-4 规模与验收

确定试点租户数、用户数、峰值并发、文件数/总页数/扫描比例、日新增、最大单文件、缓存延时和问答 P95。当前建议先用 10 万 chunks 做容量讨论基线，但不能当作用户已确认规模。

### P0-5 身份与合规

确定独立账号还是企业 SSO；明确平台管理员、租户管理员、知识管理员、FAQ 审核员和审计员人选；确认安全审计保留期。普通业务日志 7 日已经确认，但安全审计是否也为 7 日仍需单独决定。

## 3. 服务器只读盘点

正式操作前先检查并记录，不修改服务器：

- ECS 地域、实例规格、系统版本、CPU、RAM、Swap、磁盘剩余量和挂载点。
- Docker Engine、Compose plugin、Git、curl、OpenSSL 版本。
- `/root/myproject` 的所有者、权限、剩余空间，以及是否已有 `AI_Knowledge_Base` 目录。
- 当前运行容器、端口和网络；避免与电商助手的 80、5432、6379 或容器名冲突。
- 阿里云安全组、UFW、域名、证书和公网 IP 状态。
- 现有电商项目实际内存、CPU 和磁盘峰值，为 Milvus 留出容量。

这一步需要用户后续授权登录服务器时再执行。本次没有执行。

## 4. Git 仓库准备

1. 确认仓库默认分支、是否为空、可见性和分支保护策略。
2. 为知识库仓库单独创建 Deploy Key 或受限机器凭据，不复用电商项目私钥文件名和权限。
3. 服务器远端建议使用 SSH deploy URL，开发机可使用用户当前习惯；不要把 Token 写入 remote URL。
4. 确定检出目录 `/root/myproject/AI_Knowledge_Base`，不要在 `/root/myproject` 根目录直接散放仓库文件。
5. 初始化 `.gitignore`，排除 `.env`、日志、storage、backups、Milvus 数据、PostgreSQL 数据、模型缓存和真实文档。
6. 配置 main 分支保护、CI 必需检查、architecture guard 和秘密扫描。

## 5. 端口和服务命名准备

知识库项目必须使用独立 Compose project name、container name 前缀、Docker network、volume 和数据库名。即使与电商助手同机，也不能复用其 PostgreSQL/Redis 数据卷。

需要规划 Web 对外端口或域名、API 内部端口、PostgreSQL 内部端口、Redis 内部端口、Milvus gRPC/HTTP 端口以及对象存储/etcd 端口。除 Web 的 80/443 和必要的 SSH 22 外，数据库、Redis、Milvus、etcd、对象存储管理端不得暴露公网。

若两个项目都要占用 80/443，应使用统一宿主机反向代理按域名路由，而不是让两个 Compose 同时映射同一个端口。

## 6. Milvus 专项准备

- 决定 Milvus 版本并锁定 Python SDK 兼容版本。
- 确认 Standalone 的 etcd/对象存储依赖和持久化目录，或托管服务的 endpoint/VPC/备份能力。
- 设计 collection schema：chunk_id、tenant_id、kb_id、document_id、version_id、index_version、embedding、created_at。
- 用 bge-large-zh-v1.5 真实返回验证向量维度，不能只依据文档猜测后建 collection。
- 选 HNSW 或其他 ANN 与 metric 前先用代表性评测集测试 Recall@K、P95、内存和构建时间。
- 实现固定批次 upsert、进度、断点续传、幂等键和 PostgreSQL/Milvus 对账。
- 准备 collection alias 切换和旧版本观察期，禁止更新时直接 drop 当前 collection。
- 将 Milvus 数据、日志和备份纳入磁盘容量、告警和恢复演练。

## 7. 开发环境和依赖准备

沿用电商助手原则：依赖全部锁定并在容器中运行。建议后端 Python 使用 uv + lockfile，前端使用 pnpm + lockfile，CI 中执行 compile/typecheck/test/build/architecture guard。

需要准备独立 Compose 服务：postgres、redis、milvus（以及自建所需依赖）、migrate、api、agent-runtime、ingestion-worker、aiops-worker、dispatcher、scheduler、frontend。开发环境可采用 Milvus Lite 或测试容器做快速测试，但最终不能代替目标 Milvus 的集成验证。

## 8. 配置与秘密准备

创建 `.env.example`，只放占位符和非秘密默认值。真实 `.env` 只保存在服务器目标项目目录，权限 `600`；未来正式环境可迁移到阿里云 KMS/Secrets Manager。

配置组至少包括：应用和端口、PostgreSQL、Redis/Celery、Milvus endpoint/auth/database/collection、对象存储、MinerU、SiliconFlow、ModelScope/MCP、JWT、CORS、Graph checkpoint、任务 lease、模型超时、日志保留和租户配额。

启动时检查必填配置、模型超时与 lease 的关系、Milvus collection 维度、数据库迁移和 provider mode。缺少 live 密钥时不能把 mock 标成 live。

## 9. 演示数据准备

生成两个明显不同的虚构租户，并覆盖 global、父部门、子部门、角色、个人和空 ACL。文档样例包含 PDF、扫描 PDF、DOC、DOCX、Markdown、TXT、复杂表格和密码文件失败样例。

建立独立 Eval 数据集：授权检索题、跨租户攻击题、三路证据冲突题、MCP 超时题、FAQ 同义/否定题、知识缺口和调岗撤权题。数据初始化必须幂等。

## 10. CI/CD 与架构守卫准备

CI 至少执行后端格式/类型/测试、前端类型/构建、数据库迁移检查、Graph 编译、Prompt/Schema 校验、模块边界测试、敏感信息扫描和当前架构 SHA-256。

部署流程沿用 `manage.sh` 思路：`config → pull → prepare → backup → migrate → build → start → doctor → guard`。脚本需要新增 Milvus 健康、collection schema、PostgreSQL/Milvus 对账和对象存储检查。

不要直接在 ECS 修改受 Git 管理源码。更新使用 `git pull --ff-only`，失败即停止，避免覆盖服务器变化。

## 11. 备份和恢复准备

- PostgreSQL：格式化备份、权限 `0600`、异机或 OSS 副本。
- 对象文件：原文、MinerU 产物、chunk manifest 和 hash。
- Milvus：自建依赖的数据和元数据备份；托管服务快照；同时保留可重建所需的 chunk、模型和索引配置。
- Redis：可重建，不作为权限和任务唯一事实源。
- 恢复演练：先 PostgreSQL 和对象文件，再 Milvus，最后对账 active chunks；确认 tenant/ACL 后开放问答。

## 12. 推荐准备顺序

```text
确认当前 ECS 资源
 → 决定 Milvus 部署形态
 → 确认数据外发与服务账号
 → 确认规模、预算和验收指标
 → 确认 Git/Deploy Key/目录
 → 规划域名、端口、网络和数据卷
 → 生成两租户样例与 Eval 集
 → 建立 Monorepo、Compose、CI 和架构守卫
 → 最小 Provider 冒烟
 → 开始 M0/M1 实现
```

目前最先需要决定的是 Milvus 部署形态和是否升级或新增 ECS；最先需要只读验证的是当前 ECS 规格、资源余量、`/root/myproject` 和 GitHub 仓库状态。本次只完成记录，没有执行上述准备操作。

# 知识库管理平台实施状态与验证记录

版本：0.1  
日期：2026-09-15  
架构基线：v3.1 / `A15D5947F2DCD34CF8A2B5567B7B963D222D7580FA3FB9B0A4EB742432057A43`

## 已实现

| 需求域 | 当前实现 | 验证证据 |
|---|---|---|
| 多租户认证 | JWT 的 `tid` 来自服务端用户记录；请求不能用参数覆盖租户 | 两租户登录与 `/me` 冒烟通过 |
| 撤权 | 每次请求比对数据库 `authz_version` | 旧 Token 失效单元测试 |
| 部门权限 | 单直属部门，Closure Table 计算祖先授权范围 | ACL 与认证服务测试 |
| 四维 ACL | global / department / role / user，OR，默认拒绝 | 跨租户、空 ACL、各维匹配测试 |
| 文档导入入口 | 原文、版本、个人 ACL、任务与 Outbox 落库 | 上传应用服务测试 |
| 文档导入图 | 七节点 LangGraph 与 mock Provider | 格式、失败、发布测试 |
| Milvus 隔离 | upsert 带 tenant/index；search 强制过滤两者 | Milvus Client 注入测试 |
| 三路问答图 | local / MCP / general 并行，融合、写答案、引用校验 | ACL 剔除和伪造引用测试 |
| 问答留痕 | 会话、消息、证据、ModelCall 持久化 | Chat 服务测试 |
| 异步执行 | Transactional Outbox、Celery Beat、导入 Worker | 编译与 Compose 静态校验 |
| Web | 登录、知识库、上传、问答 | Vite 生产构建和浏览器可见冒烟 |
| 云部署骨架 | PostgreSQL、Redis、Milvus、etcd、MinIO、API、Worker、Web | `docker compose config --quiet` 通过 |

## 当前验证结果

- 架构守卫：PASS，架构文档 SHA-256 未漂移。
- Python 编译：PASS。
- Pytest：22 passed，只有 LangGraph 依赖的未来弃用提示。
- 前端：Vite 生产构建 PASS，28 modules transformed。
- Compose：静态配置 PASS。
- 浏览器：登录、知识库列表、问答结果与 MOCK 标识可见。
- Docker 容器：未运行；本机 Docker daemon 未启动。

## 尚未完成，不得标记为 live

1. 硅基流动 Chat / Embedding / Rerank 的真实适配器和最小授权调用。
2. MinerU 具体 API 协议、回调/轮询、扫描 PDF 与 DOC 的真实解析验证。
3. 魔搭 MCP 服务 URL、工具名、认证方式和真实工具调用。
4. Milvus collection 初始化、真实向量检索、标量 ACL 二次过滤与 Rerank。
5. PostgreSQL LangGraph Checkpoint、Redis 登录限流与 Token 黑名单。
6. FAQ 演化图、知识缺口图、审核页面、组织/角色管理完整页面。
7. 密码文件、低质量扫描件人工复核和人员权限转移 Saga。
8. Docker 镜像构建、容器健康、阿里云部署、HTTPS、备份恢复演练。

## 下一实施批次

按依赖顺序继续：Live Provider 适配器及契约测试 → Milvus 初始化和授权检索 → 导入任务状态流/SSE → FAQ 与知识缺口 Graph → 管理页面 → Docker 实机 → 阿里云试点验收。

# 知识库管理平台需求规格说明书

文档版本：v1.1

日期：2026-09-14

文档状态：企业内部试点需求基线

需求来源：`项目实战.md` 第 2.9 节，以及 2026-09-14 用户补充确认

## 1. 文档目的

本文把“知识库管理平台”的业务目标拆成可开发、可测试、可验收的需求。本文是产品、研发、测试、运维和试点负责人共同使用的需求基线；最新且唯一的技术实现基线见《02_知识库管理平台_标准AI项目架构设计说明书.md》v3.1。

当前确认的方向如下：

- 系统面向企业内部试点，采用多租户模式。
- 一个用户在一个租户内只能属于一个直属部门；部门授权默认覆盖该部门及所有下级部门。
- 文档支持 PDF、Markdown、TXT、DOCX 和旧版 DOC；解析能力通过 MinerU API 适配层接入。
- 目前没有真实业务数据，项目必须生成脱敏、虚构、可复现的演示数据。
- Chat 模型使用硅基流动 `deepseek-ai/DeepSeek-V3`。
- Embedding 模型使用硅基流动 `BAAI/bge-large-zh-v1.5`。
- Rerank 模型使用硅基流动 `Pro/BAAI/bge-reranker-v2-m3`。
- 问答采用三路方案：本地知识库检索、通过魔搭平台 API 接入的 MCP 服务、LLM 自有知识生成，三路融合后产生最终答案。
- 部署目标为云服务；业务日志保留 7 日。
- 设租户最高管理员 `tenant_admin`，可把 FAQ 审核、审计查看等功能权限授予其他角色。
- 目标部署环境与“电商运营助手”一致：阿里云 ECS、Docker Compose、Nginx、FastAPI、PostgreSQL、Redis、Celery、LangGraph PostgreSQL Checkpoint 和 Transactional Outbox；宿主机不单独安装 Python、Node.js、PostgreSQL、Redis 或 Nginx。
- 向量数据库使用 Milvus；PostgreSQL 继续作为业务、权限、任务和发布状态的权威事实源。
- 阿里云项目根目录已确认为 `/root/myproject`；建议仓库检出目录为 `/root/myproject/AI_Knowledge_Base`，实施前需在服务器上确认最终目录名。
- GitHub 仓库已由用户创建：`https://github.com/feihu0608/AI_Knowledge_Base.git`。本次只记录，尚未验证远端可访问性、默认分支、权限或 Deploy Key。

模型名称是用户指定的配置目标。实施阶段必须用获授权的最小调用验证模型名称、接口兼容性、账户可用性、限额与返回结构，验证前不能标记为“已接通”。

## 2. 产品目标

平台要让多个企业或组织在同一套系统中安全管理各自知识，完成下列闭环：

```text
租户开通
  → 组织、用户和角色配置
  → 文档导入和 MinerU 解析
  → 切片、向量化、索引
  → 四维数据权限配置
  → 本地知识 + MCP + LLM 三路问答
  → 引用、权限提示和审计
  → 高频问题聚类
  → FAQ 人工审核、发布和缓存
  → 知识缺口转补充任务
  → 新文档入库并复验
```

试点成功的核心不是页面数量，而是不同租户、部门、角色和个人之间没有越权泄漏，并且用户能从真实上传动作走到有来源的最终回答和可追踪的知识沉淀结果。

## 3. 范围

### 3.1 首期必须交付

1. 多租户隔离、租户管理员、用户、部门树、角色和功能权限。
2. 单文件、批量文件和文件夹导入。
3. PDF、Markdown、TXT、DOCX、DOC 的解析任务、进度、失败处理和重试。
4. 文档清洗、切片、Embedding、关键词索引、版本和生命周期管理。
5. global、department、role、user 四维数据权限及 OR 放行逻辑。
6. 本地混合检索、MCP 服务检索、LLM 自有知识三路融合。
7. 多轮对话、SSE 流式输出、Markdown、代码块、引用卡片、受限提示和历史会话。
8. 问答审计、PV/UV、知识量、热门问题、热门知识、Token 和时延看板。
9. 高频问题聚类、FAQ 候选、提交、审核、发布、驳回、缓存和失效。
10. 知识缺口识别、聚合、建任务、补文档、复验和关闭。
11. 虚构演示数据和至少一个浏览器端到端业务演示。
12. 云部署配置、健康检查、日志、备份恢复、迁移和回滚说明。

### 3.2 首期不自动承诺

下列能力只有在后续明确需求后才进入范围：与真实 AD/LDAP/OIDC 单点登录集成、外部网盘自动同步、扫描件人工校对工作台、复杂知识图谱、多地域容灾、跨租户知识共享、移动端 App、互联网匿名访问、正式生产 SLA。

### 3.3 数据与效果边界

- 演示数据必须全部虚构，不使用真实姓名、薪资、合同、手机号、身份证号或企业机密。
- LLM 生成内容不是权威事实源。内部制度、金额、适用范围、审批条件必须由本地或受信任 MCP 证据支撑。
- MCP 服务返回内容属于外部证据候选，必须记录服务、工具、调用参数摘要、返回时间和引用，不能默认认为真实。
- 页面出现成功提示不等于功能完成；数据库记录、任务终态、可见结果和审计记录必须一致。

## 4. 术语

| 术语 | 定义 |
|---|---|
| 平台管理员 | 管理整个平台租户生命周期的运维角色；默认不能读取租户正文 |
| 租户管理员 | 某租户最高管理角色，可配置组织、角色和功能权限；默认不自动读取全部正文 |
| 知识管理员 | 在被授权知识库范围内导入、编辑、授权、启停和删除知识 |
| 审核员 | 获得 `faq.review` 或相关审计权限的用户，可由租户管理员赋权 |
| 直属部门 | 用户在租户内唯一所属部门 |
| 部门子树 | 某部门及其所有后代部门 |
| 功能权限 | 是否可以执行上传、授权、审核等动作的 RBAC 权限 |
| 数据权限 | 是否可以读取具体文档、切片、FAQ、引用或历史内容的 ACL 权限 |
| 本地检索 | 对本租户内部知识文档执行关键词、向量和 rerank 检索 |
| MCP 检索 | 通过受控 MCP 网关调用魔搭平台提供或托管的 API 工具获取外部证据 |
| LLM 自有知识 | DeepSeek-V3 不依赖检索证据产生的一般知识，必须在最终答案中明确标注 |
| 知识单元 | 管理界面中的一篇逻辑文档或一条已发布 FAQ |
| 切片 | 文档版本内用于检索和引用的最小文本单元 |

## 5. 角色与权限

### 5.1 平台级角色

| 角色 | 能力 | 默认禁止 |
|---|---|---|
| platform_admin | 创建、停用租户，查看平台健康和租户级汇总用量 | 读取任意租户正文、问题原文和答案正文 |
| platform_operator | 处理部署、任务积压和基础设施告警 | 修改租户业务角色和读取业务正文 |

### 5.2 租户级角色

| 角色 | 主要功能权限 | 数据读取范围 |
|---|---|---|
| tenant_admin | 用户、部门、角色、系统参数、授权委派、租户看板 | 仍按文档 ACL；不因是管理员自动读全部正文 |
| knowledge_admin | 知识导入、编辑、切片、ACL、启停、缺口任务 | 被分配知识库的管理预览范围；访问须留审计 |
| faq_reviewer | 查看允许范围内的 FAQ 候选、审核、驳回、发布 | 只看其管理范围和来源仍可访问的内容 |
| audit_viewer | 查看获授权级别的问答审计和运营明细 | 按审计范围脱敏；不自动读取被拦截正文 |
| normal_user | AI 问答、本人会话、允许的引用 | 严格按四维 ACL |

`tenant_admin` 可以创建角色并授予功能权限，但不能授予自己超出租户的能力。修改敏感知识 ACL、临时读取敏感正文等高风险操作必须记录操作者、原因、修改前后值和时间。

### 5.3 知识管理员和系统管理员的暂定规则

用户尚未给出明确规则，因此首期采用最小权限默认值：

- 知识管理员只管理 `kb_manager_scope` 中明确分配的知识库。
- 管理预览是独立权限，不等于普通问答读取权限；每次预览正文都记录审计。
- 租户管理员和平台管理员都不自动获得全部文档阅读权。
- 如企业确需紧急读取，使用限时 `break_glass` 授权：申请原因、审批人、有效期、访问对象和访问日志必须完整保存。
- 试点期间可由 tenant_admin 审批限时授权；正式上线前建议要求另一位审批人，避免自批自读。

## 6. 多租户与组织需求

### FR-TEN-001 租户隔离

- 每个租户有唯一 `tenant_id`、状态、名称、配置和密钥引用。
- 所有租户业务表必须带 `tenant_id`；唯一约束、外键、缓存键、对象存储路径、队列任务和审计查询均包含租户边界。
- API 不信任客户端提交的 tenant_id；服务端从认证上下文确定租户。
- 用户、角色、部门、文档、向量、会话、FAQ、缺口和统计禁止跨租户关联。
- 对不可见的其他租户对象统一返回 404 或受控结果，不透露对象是否存在。

验收：租户 A 用户即使构造租户 B 的文档、会话、引用、下载、FAQ 和任务 ID，也不能读取、修改或从搜索结果推断其存在；自动化隔离测试为 0 次泄漏。

### FR-ORG-001 部门树

- 一个租户可维护多级部门树，部门名称在同一父级下唯一。
- 禁止形成循环，禁止把部门移动到自身后代节点。
- 用户在一个租户内只能绑定一个有效直属部门。
- 部门授权覆盖本部门及全部下级部门；例如授权“华东区”后，华东区及其下属上海、杭州部门用户都满足 department 条件。
- 部门移动、用户调岗后，新请求立即按新祖先链鉴权；缓存必须失效。

验收：构建三层部门树，父部门授权可放行后代用户；兄弟部门、祖先部门用户不能反向获得权限；调岗提交后的新请求使用新权限。

### FR-ORG-002 人员离职、调岗与权限转移

- 离职：先禁用账号、增加 `authz_version`、撤销会话，再转移其知识管理职责、待审核任务、缺口任务和个人专属知识处置责任。
- 调岗：变更唯一直属部门、增加权限版本、使组织路径缓存失效；其角色是否保留由管理员明确选择。
- 管理职责转移必须选择目标人员，形成 transfer 批次，并记录旧负责人、新负责人、对象清单、原因和结果。
- 个人专属知识默认不会自动改成新负责人可读；管理员必须选择“保持封存、转移个人 ACL、改为部门 ACL”之一。
- 已经发送到浏览器或下载的内容不能收回；历史会话、引用、FAQ 和继续追问在再次访问时按当前权限重新校验。

验收：禁用用户的现有令牌失效；转移任务可追踪并可重试；新负责人仅获得明确选择的职责和 ACL，不能因为接手任务读取无关个人知识。

## 7. 功能需求

### FR-AUTH 身份与功能权限

1. 用户通过账号密码登录；未来可替换为企业 SSO。
2. 密码使用安全哈希，登录失败限速，禁用用户拒绝登录。
3. Access Token 短时有效，Refresh Token 可撤销；令牌包含 user_id、tenant_id 和版本，不长期信任客户端角色列表。
4. 后端所有敏感接口校验功能权限；前端按钮显隐仅用于体验。
5. tenant_admin 可创建角色并勾选菜单和操作权限，例如 `knowledge.upload`、`knowledge.acl.update`、`faq.review`、`audit.read`。
6. 权限修改后，新请求立即按新权限执行，已有长连接在关键输出点重新校验。

### FR-KB-001 知识库管理

- 租户可创建多个知识库，设置名称、分类、负责人、管理成员和启用状态。
- 知识管理员只能看到其管理范围内的知识维护页面。
- 知识台账展示编号、标题、格式、分类、ACL 标签、当前版本、解析状态、启用状态和更新时间。
- 支持文档新增、查看元数据、创建新版本、编辑切片、启停、软删除和恢复。
- 删除/禁用后先阻止新查询，再异步清理索引和缓存；审计记录保留。

### FR-IMP-001 文件导入

- 支持单文件、多个文件和浏览器文件夹拖拽导入。
- 支持 `.pdf`、`.md`、`.txt`、`.docx`、`.doc`。
- 前端检查扩展名、大小和数量；后端检查真实媒体类型、文件头、路径穿越、空文件和租户配额。
- 每次导入创建 batch，每个文件创建独立 job；批次允许部分成功。
- 进度显示真实阶段和数量：排队、上传、转换、MinerU 解析、清洗、切片、向量化、索引、完成。
- 用户可取消尚未执行的工作；运行中任务在安全检查点停止。
- 同租户同知识库内用文件 hash、解析版本和分块版本识别重复；不能跨租户复用业务对象。

### FR-IMP-002 MinerU API 解析

- 所有复杂文档解析通过 `MinerUProvider` 适配层，业务代码不直接依赖具体 URL。
- 适配层支持提交、查询、取消（若服务支持）、下载结果、超时、限流、重试和回调/轮询两种模式。
- `.doc` 若 MinerU 当前接口直接支持则直接提交；若不支持，先在隔离转换进程转为 `.docx` 或 PDF，再提交 MinerU，并保存原文件和转换产物 hash。
- 扫描 PDF 通过 MinerU 的 OCR 能力处理；若服务返回无文本或低质量状态，标记 `needs_review`，不得显示为解析成功。
- 复杂表格必须保留表格与页码关系；切片时重复必要表头，防止字段和值脱离。
- 密码保护文件不得尝试破解。任务状态为 `password_required`，提示用户重新上传已解密且有权处理的副本；密码不写日志。
- 解析结果属于不可信输入：清理脚本、外链、嵌入文件和可能的提示注入文本，只保存允许的结构化内容。
- 实施时必须查明 MinerU API 的真实文件格式、大小、异步任务、结果格式和费用限制，本文不虚构接口地址。

### FR-IDX-001 清洗、切片和索引

- 保存原文件、文档版本、解析产物、parser_version、chunker_version、embedding_model 和索引版本。
- 清洗重复页眉页脚和异常空白，同时保留标题层级、段落、页码、表格位置和字符偏移。
- 首始参数建议 500—800 tokens、重叠 80—120 tokens，最终值必须通过评测确定。
- 使用硅基流动 `BAAI/bge-large-zh-v1.5` 生成向量，校验返回数量、维度、NaN/Inf 和模型标识。
- 建立中文关键词索引和向量索引；文档版本全部就绪后才能原子切换为 active。
- Embedding 或索引失败不影响旧版本继续服务；失败版本可从保存阶段恢复。

### FR-ACL-001 四维数据权限

允许读取的确定性规则为：

```text
authenticated
AND same_tenant
AND feature_permission_if_required
AND document_enabled_and_version_active
AND (
  global_for_current_tenant
  OR user.department is in descendants(including self) of acl.department
  OR intersection(user.roles, acl.roles) is not empty
  OR user.id is in acl.users
)
```

- 四维采用 OR；没有任何 ACL 默认拒绝。
- global 只表示当前租户所有有效登录用户，不表示互联网公开，也不跨租户。
- 用户可同时拥有多个角色，但只能有一个直属部门。
- 创建者、上传者、tenant_admin 不构成隐式第五种数据权限。
- ACL 更新采用乐观锁和版本号；提交后写 outbox 事件，使问答、引用和 FAQ 缓存失效。
- 所有搜索、推荐问题、引用、预览、下载、历史会话、导出、FAQ、审计明细都必须使用统一鉴权服务。

### FR-QA-001 AI 问答工作台

- 显示会话列表、新建会话、问题输入、停止生成、重新连接和历史加载。
- 使用 SSE/Fetch Stream 输出，支持 Markdown、代码高亮、复制和引用卡片。
- 每次问答必须有 `request_id` 和客户端幂等键，重复提交不能重复计费。
- 记录 accepted、retrieving、generating、completed、denied、no_evidence、failed、cancelled 状态。
- 引用卡片包含来源类型、本地文档标题/页码或 MCP 服务/资源标识；打开和下载再次鉴权。
- 有受限本地候选但无足够授权证据时，使用固定提示，不显示受限标题、摘要、数量或正文。

### FR-QA-002 三路检索与融合

三路可并行执行，但必须分别设置策略、超时和证据等级：

1. 本地知识路：对当前 tenant 的关键词和向量结果融合，执行 ACL 过滤，再对授权片段调用 `Pro/BAAI/bge-reranker-v2-m3`。
2. MCP 路：由 MCP 网关从租户允许的工具白名单中选择魔搭 API 服务，校验参数，调用并规范化外部证据。
3. LLM 路：调用 `deepseek-ai/DeepSeek-V3` 生成一般知识候选。它可以帮助解释，但没有可验证来源时标记 `model_knowledge`。

融合规则：

- 内部制度、薪资、金额、权限、合同、审批流程，只能由授权的本地证据或已批准的 MCP 权威来源支持。
- 本地证据与 MCP 证据冲突时不静默合并；回答列出冲突、来源和时间，并提示由负责人确认。
- LLM 自有知识不能覆盖本地制度，也不能填补受限知识内容。
- 最终输出经过结构化 Schema 验证，引用 ID 必须属于本次证据白名单。
- 每个回答显示“企业知识”“外部 MCP”“模型一般知识”的来源标签；无证据句不得挂虚假引用。
- 用户可在租户策略允许时选择“仅企业知识”“企业知识 + MCP”“三路综合”。敏感分类可强制仅本地。

### FR-MCP-001 魔搭 MCP 服务

- 每个租户配置可用 MCP server/tool 白名单、用途、参数 Schema、数据分类上限、超时和每日调用限额。
- 魔搭平台 API 通过 MCP Adapter 封装：若目标本身是 MCP server，使用标准客户端；若仅是普通 API，则由受控 MCP server wrapper 暴露为工具。
- 模型不能自由拼接 URL、密钥或任意调用工具；编排器根据策略调用已注册工具。
- 查询参数执行 Schema 校验、敏感字段检查和租户外发策略；返回内容限制大小并消毒。
- MCP 调用记录 server_id、tool_name、参数摘要 hash、响应摘要 hash、耗时、状态和引用元数据，不记录密钥。
- MCP 失败时按错误类型有限重试；失败不阻断有足够本地证据的回答，界面说明外部来源暂不可用。

### FR-FAQ-001 高频问题聚类

“正常频次、正常窗口”在首期定义为租户可配置默认值：7 天滑动窗口、有效问题至少 10 次、独立用户至少 5 人。它是可执行的产品默认值，试点后按数据调整。

- 排除失败请求、重复幂等请求、测试流量和明确拒绝统计的请求。
- 频次同时显示有效请求数和独立用户数，不能用同一用户刷问达到候选门槛。
- 聚类在 tenant 范围内进行，并进一步按问题和来源的可见范围限制运营人员查看。
- 候选包含代表问题、相似问法、频次、人数、时间窗、关联来源、建议答案、评分和算法版本。

### FR-FAQ-002 审核、发布和缓存

- 候选状态：draft → submitted → approved/rejected → published → invalidated/archived。
- tenant_admin 可将 `faq.review`、`faq.publish` 权限赋予其他角色。
- 提交人、审核人、决定、理由、时间、修改前后内容和来源版本必须审计。
- 已发布 FAQ 的可见范围不能大于其依赖来源共同允许的范围；读取时 FAQ ACL 和所有依赖来源 ACL 都必须通过。
- Redis 只保存加速副本。命中缓存后仍校验租户、FAQ 版本、ACL 版本和来源状态。
- 来源更新、撤权、禁用、FAQ 编辑或模型/规范化规则变更触发缓存失效。

### FR-GAP-001 知识缺口

- `no_match`、`low_relevance`、`insufficient_evidence` 可形成知识缺口。
- `access_denied` 不能自动算作知识缺口；`provider_error` 进入系统告警。
- 缺口按租户、允许的管理范围和相似问题聚合，显示问题、部门、频次、人数、最高相似度和建议分类。
- 管理员可转为知识补充任务，指定负责人、截止时间和知识库。
- 补充文档后，用原问题和原测试身份复验；检索到正确授权来源且人工确认后才关闭。

### FR-ANA-001 运营看板与日志

- 业务日志在线保留 7 日，数据库时间使用 UTC，页面按租户时区展示。
- 7 日后业务正文、查询原文和普通运行日志按清理策略删除或匿名化；聚合指标可长期保留，但不能反推出个人问题内容。
- 安全审计日志与业务日志是否同样只保留 7 日尚未获确认。首期建议安全审计至少保留 180 日，并把期限配置化；正式试点前由企业负责人确认合规要求。
- 看板提供工作台 PV、问答 UV、有效请求数、文档/FAQ/chunk 数、热门问题、热门引用、FAQ 命中率、各模型 Token、各阶段 P50/P95 和失败率。
- Token 缺失标记 unknown，不能写成 0。指标按 request_id 去重并可与数据库事件对账。

## 8. 非功能需求

### 8.1 安全

- 跨租户和四维权限自动化测试中允许泄漏次数为 0。
- 所有外部模型和 MCP 调用经 AI Gateway；租户敏感数据是否允许出云边界由策略决定。
- API 密钥仅保存在云密钥管理服务或受保护环境，不出现在源码、浏览器、日志、截图和文档。
- 上传文件进行大小、类型、恶意内容和路径检查；解析在隔离进程或容器执行。
- 防御文档提示注入、MCP 工具注入、引用伪造、IDOR、SQL 注入、XSS 和 SSE 内容注入。

### 8.2 性能候选基线

真实规模尚未给出，以下作为试点起始验收值，压测前可由负责人调整：

| 场景 | 候选目标 |
|---|---|
| 热 FAQ 完整 API | P95 ≤ 300ms，不含浏览器网络抖动 |
| 本地检索 | 10 万 chunk 条件下 P95 ≤ 800ms |
| 三路问答首 token | 正常网络下 P95 ≤ 5s |
| 三路问答总时长 | 95% 请求 ≤ 20s |
| ACL 修改生效 | 数据库提交后新请求立即生效，缓存失效目标 ≤ 2s |
| 上传任务可见 | 接收后 2s 内显示持久化任务 ID |

所有报告必须注明云实例规格、并发、模型、网络、文档量和冷热缓存。

### 8.3 可用性与恢复

- 试点采用至少每日全量备份、持续或定时增量备份；候选 RPO 24 小时、RTO 4 小时，正式值待企业确认。
- API、worker、scheduler、数据库、Redis、对象存储和外部 provider 分层健康检查。
- 队列任务可幂等重试；旧有效文档版本在新版本失败时继续服务。
- 外部 MCP 不可用时，本地知识仍可回答；Chat 模型不可用时返回检索证据列表和明确状态，不生成假答案。

### 8.4 可维护性

- 数据库变更使用迁移；Prompt、Schema、分块器、解析器和索引均有版本。
- provider 访问统一适配，不在业务模块散落供应商请求代码。
- 日志用 request_id、job_id、tenant_id 关联，正文默认不写应用日志。

## 9. 演示数据要求

生成至少两个租户，才能证明多租户隔离：

### 租户 A：星河商贸（虚构）

- 部门：总部 → 财务部、人力资源部、业务中心 → 华东区 → 上海组。
- 角色：普通员工、管理层、知识管理员、FAQ 审核员、租户管理员。
- 文档：差旅报销标准（global）、高管薪酬细则（HR 部门 OR 管理层）、上海客户折扣（上海组）、个人培训计划（指定 user）、无 ACL 草稿、生鲜退款流程。
- 缺口：海外直邮清关延误。

### 租户 B：青岚制造（虚构）

- 使用完全不同的部门、用户、知识标题和正文。
- 文档 ID、FAQ 问法可以设计成与租户 A 相似，用于测试缓存键和检索隔离。

文档应覆盖五种格式，并包含文本 PDF、扫描 PDF、表格、旧版 DOC 和密码文件失败样例。所有内容显著标注“虚构演示数据”。

## 10. 验收场景

| ID | 操作 | 预期结果 |
|---|---|---|
| AC-01 | 租户 A 用户构造租户 B 的 ID 查询各类资源 | 所有读取和修改失败，搜索、缓存、看板也无 B 数据 |
| AC-02 | 给业务中心授权，上海组用户和财务用户分别问相关问题 | 上海组通过，财务不因同租户而通过 |
| AC-03 | 用户从上海组调到财务部后重新请求 | 原部门授权失效，财务授权生效，历史引用重新鉴权 |
| AC-04 | 导入五种格式、扫描 PDF、复杂表格和密码文件 | 支持格式真实解析；密码文件明确失败；进度和产物可追踪 |
| AC-05 | 普通员工询问薪酬，管理层再询问 | 普通员工无泄漏；管理层得到有来源答案 |
| AC-06 | 同时启用本地、MCP 和 LLM 三路 | 最终答案按句/段标出来源类型，冲突不被隐藏 |
| AC-07 | MCP 超时、本地有足够证据 | 回答继续完成并显示 MCP 不可用；审计记录超时 |
| AC-08 | 本地无证据，LLM 给出确定内部金额 | 输出校验阻止把该金额作为内部事实，提示证据不足 |
| AC-09 | 达到 FAQ 默认频次并授权审核员发布 | 候选真实产生、审核有审计、命中缓存且不调用 Chat |
| AC-10 | 撤销 FAQ 来源权限后再次命中 | 缓存不能返回旧答案，按当前 ACL 处理 |
| AC-11 | 无资料问题转缺口、补文档、复验 | 任务闭环，只有原身份正确命中后关闭 |
| AC-12 | 员工离职并转移职责 | 旧令牌失效，任务归属转移，个人知识按明确选择处置 |
| AC-13 | 超过日志保留期运行清理 | 业务明细按策略消失，聚合值保留且不可反查原文 |
| AC-14 | 云环境重启 worker/Redis/API | 权威数据不丢，任务可恢复，权限不依赖旧缓存 |

## 11. 需求到测试矩阵

| 需求组 | 单元测试 | 集成测试 | 浏览器/真实服务验证 |
|---|---|---|---|
| 多租户/组织 | tenant_id 约束、部门闭包、调岗 | 跨租户 API/数据库、缓存键 | 两租户 UI 隔离和调岗后重试 |
| ACL/RBAC | 四维真值表、角色权限 | 搜索/引用/历史/FAQ 全入口 | 多身份端到端录屏 |
| MinerU 导入 | 格式路由、状态机 | 经授权的真实 API 调用、失败和轮询 | 上传到可检索结果 |
| 三模型 | 输入/Schema/错误分类 | 硅基流动三个模型各一次成功和受控失败 | 最终答案、引用和 usage 可见 |
| MCP | allowlist、参数验证 | 魔搭目标 API 成功、超时、错误返回 | 开启/关闭工具和来源卡片 |
| FAQ/缺口 | 阈值、状态机、来源交集 | 聚类、审批、缓存失效、复验 | 管理页面完整闭环 |
| 看板/保留 | 指标公式、过期计算 | SQL 对账、清理任务 | 页面筛选与刷新后持久 |
| 部署恢复 | 配置校验 | PostgreSQL/Milvus/对象文件一致备份恢复、任务重投 | 阿里云访问和重启证据 |

所有测试当前均为待实现、待执行。Mock 用于异常注入，不替代 MinerU、硅基流动模型和魔搭 MCP 的真实授权冒烟测试。

## 12. 已定决策与待确认项

已定：多租户；单直属部门；部门授权覆盖下级；企业内部云试点；五类文档；MinerU API；硅基流动三种模型；本地 + MCP + LLM 三路；魔搭 API；日志 7 日；最高管理员可委派审核权限。

仍待企业确认：阿里云地域和 ECS 规格、Milvus 自建或独立/托管形态、域名/HTTPS、实例和 GPU、并发/容量、MinerU 具体服务形态、魔搭 MCP 工具清单、数据能否发送给硅基流动和魔搭、模型预算/限流、SSO、正式 RPO/RTO、安全审计保留期、管理员紧急读取审批人、交付期限。

这些待确认项不阻止先用虚构数据开发，但会阻止真实企业数据接入和生产化验收。

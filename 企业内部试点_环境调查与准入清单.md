# 企业内部试点：环境调查与准入清单

> 更新说明（2026-09-14）：用户已确认多租户、阿里云部署、`/root/myproject` 项目根目录、GitHub 仓库、Milvus、7 日业务日志、MinerU 和硅基流动/魔搭方向。容量、Milvus 部署形态、数据外发许可、具体 MCP 工具、预算和安全审计期限仍需在真实数据接入前补齐。

版本 v0.1｜2026-09-14。用户已明确选择“企业内部试点，需要按真实环境进一步细化”。本文补充主分析文档的企业落地边界；尚未收到真实环境参数，不声称已检查企业服务器或基础设施。

## 1. 当前已知与未知

已知：目标是第 2.9 节知识库平台，交付当前阶段为实现分析；使用场景是企业内部试点。原文明确四维 OR 权限、默认拒绝、鉴权问答、FAQ 审批缓存和知识缺口。

已确认环境沿用电商运营助手：阿里云 ECS、Docker Compose、Nginx、FastAPI、PostgreSQL、Redis、Celery、LangGraph PostgreSQL Checkpoint、Outbox，宿主机不单独安装应用运行时。向量库改用 Milvus。电商运营助手环境记录为 2 核 4 GiB，但本次尚未登录服务器复核，信息可能变化。

未知：试点企业/部门、数据分级、外部模型许可、账号体系、用户规模、文档规模、预算、工期、备份恢复目标、当前 ECS 实际规格、Milvus 部署形态。这些信息决定容量和真实数据准入，不能用开发者电脑配置替代企业环境。

## 2. 需要补充的真实参数

| 参数 | 请填写的内容 | 对实现的影响 |
|---|---|---|
| 试点范围 | 部门、业务负责人、知识负责人、参与人数 | 确定首批 ACL 和管理范围 |
| 身份源 | 独立账号 / AD / LDAP / OIDC / 其他 SSO | JWT 可作为应用内部令牌，不能据原文排除 SSO |
| 组织映射 | 直属部门、多部门、角色来源、离职禁用同步时限 | 确定组织关系与权限失效机制 |
| 文档资产 | 格式占比、文件数、总容量、最大文件、页数、扫描比例 | 选择解析、OCR、批次限额与容量 |
| 更新方式 | 管理员上传频率、是否需要外部文件库同步 | 原文只保证导入，不默认包含网盘连接器 |
| 数据边界 | 公开/内部/敏感分类；是否允许问题、正文、向量出内网 | 决定模型部署位置和调用策略 |
| 模型服务 | Chat/Embedding/Rerank/OCR 可用服务及接口协议 | 决定适配、向量维度、限流和质量评测 |
| 部署资源 | 复核阿里云 ECS 的 OS、CPU、RAM、磁盘、Swap、Docker/Compose 版本 | 决定 API、Worker 与 Milvus 是否能同机运行 |
| Milvus | 自建 Standalone、独立 ECS 或托管；版本、容量、备份、网络 | 决定资源、持久化、运维和恢复方案 |
| 网络 | 外网访问、代理、域名、HTTPS 证书、内部端口 | 决定模型连通性和访问入口 |
| 负载 | 日问答量、同时请求数、导入高峰和后台窗口 | 决定连接池、队列和容量压测 |
| 运营要求 | 服务时段、负责人、预算上限、告警渠道 | 决定降级与值守策略 |
| 恢复与保留 | 可接受数据丢失时间 RPO、恢复时长 RTO、日志/备份期限 | 决定备份频率、恢复演练和清理流程 |

无需提供密钥或真实敏感资料即可完成第一轮选型。可提供脱敏的文档结构、数量与接口协议；实际凭据仅在实施时放入受保护配置。

## 3. 按数据边界选择部署方案

| 条件 | 候选拓扑 | 需要验证 |
|---|---|---|
| 正文、问题和向量全部禁止出内网 | 内网 Web/API/DB/Redis/文件服务，加内网 Chat、Embedding；OCR/Rerank 也在内网 | GPU/模型吞吐、中文效果、运维资源和许可 |
| 允许调用指定企业模型网关 | 数据与检索在内网，模型统一走企业网关 | 网关认证、日志/留存、数据分类策略、限流和审计 |
| 仅部分资料允许外发 | 内网数据分级，调用前由确定性规则筛选；敏感请求只能路由到获准服务 | 不仅最终答案，Embedding、问题改写、聚类、OCR、Rerank 都须遵守同一外发政策 |
| 全部试点数据允许使用批准的云模型 | 内网业务系统 + 批准的云端模型 API | 企业批准范围、数据处理条款、可用性、账单归属 |

不能仅把敏感片段从 Chat Prompt 删除，却提前把整份文档发给云端 Embedding 或 OCR。外发限制需要落在模型网关接口和文档分类元数据上，而不是靠管理员口头记忆。

## 4. 权限需要企业负责人确认的语义

1. global 是本企业所有已登录人员，还是还包含外部协作者？如果存在多个企业/租户，所有实体和查询需要 tenant_id 隔离，禁止 global 跨租户放行。
2. 部门权限是只允许直属部门，还是允许下级部门？部门迁移和人员调动如何同步，最迟多久生效？
3. 知识管理员可以修改哪些资料及 ACL？修改 ACL 是否允许把敏感文档扩大为 global？建议加数据分类约束，敏感资料扩权走受审计审批。
4. 系统管理员能配置系统不代表能阅读薪酬正文。默认不授予全量阅读；若需应急访问，单独定义审批、有效期和审计。
5. FAQ 发布是否必须由另一人审核？哪些人员有权查看问题原文、被拦截的文档 ID 和薪酬相关聚类？
6. 撤权后已保存的历史回答如何展示？建议对来源重新鉴权并隐藏不再可读的内容，约定已发送数据无法收回。
7. 原文受限提示会透露“存在相关资料”。企业是否接受该提示？若不接受，需明确记录为原文要求变更，采用统一无可用资料提示。

这些问题未确认前，主文档相关设计保持候选状态，不能进入真实敏感数据导入阶段。

## 5. 容量与成本估算方法

先抽样 20—50 份经批准的代表性文档，统计平均页数、清洗后 token、chunk 数、解析时间和失败类型，再外推总体规模；扫描件与文本型文档分开估计。

向量裸数据约为 `chunk 数 × 向量维度 × 每元素字节数`。例如 float32、10 万 chunk、1024 维约 0.41 GB（十进制），这仅是向量本体，不含索引、行数据、文本、原文件、版本、日志和备份。不能据此声称总内存只需 0.41 GB。

导入成本包含全文 Embedding、可能的 OCR、失败重试和后续重建；在线问答成本包含查询向量、问题改写、Rerank、输入上下文与输出；运营成本还包含定时聚类和候选生成。每次调用记录 request_id/job_id 与模型 usage，按获批价格计算。

问答并发与生成时长共同决定模型容量；采用限流和排队时应显示真实等待状态。先测单请求，再测批准的峰值并发，不在未知生产服务上直接打压测流量。

## 6. 从开发验证到受控试点

| 阶段 | 数据和参与者 | 退出条件 |
|---|---|---|
| P0 需求与环境确认 | 脱敏统计、企业负责人 | 第 2—4 节关键项明确，数据外发和权限规则有记录 |
| P1 隔离验证 | 自造数据、开发/测试身份 | 原文 R01—R10 链路与异常测试通过，有真实获准模型调用 |
| P2 小范围试点 | 经批准的资料与指定人员 | 真实身份映射、权限撤销、FAQ 失效、备份恢复验证通过 |
| P3 观察与评估 | 固定评测集与试点使用日志 | 用户问题解决率、权限结果、延时、成本达到已确认阈值 |
| P4 决策 | 业务、知识、IT 负责人 | 明确扩大、继续改进或停止；内部试点成功不直接等于生产验收 |

阶段不是新的自动授权：本次仅交付分析文档，没有获准连接企业身份系统、上传企业文档、发布服务或产生模型费用。

## 7. 试点准入与退出条件

准入至少要求：确定试点名单与资料清单；权限真值表通过；真实账号禁用与撤权验证通过；所有模型调用满足数据边界；引用、FAQ 和历史入口无已知越权；模型失败和队列故障可见；完成一次备份恢复演练；有指定问题接收与处理负责人。

出现确认的数据越权时，应停用受影响问答/缓存路径并保留必要审计，修复后重跑权限回归再恢复。出现模型不可用时保留已授权的数据库检索或 FAQ 能力并说明状态，不把未经批准的其他供应商当自动备选。费用达到已确认上限时停止新费用请求并保留已完成结果。

试点结束需形成：原文需求完成度、测试证据、真实负载下的 P95 与失败率、模型成本、已关闭/未关闭知识缺口、用户反馈和下一阶段决策。所有数值来源可追踪；未测项保留为未验证。

## 8. 已记录的代码与部署位置

- 阿里云项目根目录：`/root/myproject`，用户确认已经建立。
- 建议知识库仓库目录：`/root/myproject/AI_Knowledge_Base`，实施前只读确认是否已存在及是否为空。
- GitHub 远端：`https://github.com/feihu0608/AI_Knowledge_Base.git`，用户确认仓库已经建立。
- 本次没有登录阿里云、检查 `/root/myproject`、访问 GitHub、配置 Deploy Key、克隆代码或创建服务。
- 部署管理方式沿用电商运营助手的 `manage.sh` 思路；知识库项目实施时应独立编写，不能直接复用旧项目脚本而不检查服务名和数据卷。

## 9. 本次建议的下一步

先完成《03_知识库管理平台_开发前准备工作清单.md》中的 P0 决策和只读环境盘点，再开始代码。随后完成一条涉及公开制度与敏感制度的安全问答纵向链路，优先验证组织同步、Milvus 租户过滤、权限过滤和撤权；通过后扩展 FAQ、缺口与运营。
